<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="10dp"
    android:weightSum="1"
    tools:context=".AddToDoFragment">

    <!-- This textview indicates what the spinner is for. -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Select a category"/>

    <!-- This spinner is for selecting the preset category for the todoitem -->
    <Spinner
        android:id="@+id/spinner_add"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content" />

    <!-- This edit text is for setting the description -->
    <EditText
        android:id="@+id/toDo"
        android:layout_width="match_parent"
        android:layout_height="0dip"
        android:layout_weight=".1"
        android:layout_marginBottom="10dp"
        android:hint="Enter To Do" />

    <!-- The datepicker brings up the date for the due date -->
    <DatePicker
        android:id="@+id/datePicker"
        android:layout_width="match_parent"
        android:layout_height="0dip"
        android:layout_weight=".8" />

    <!-- This is the button that adds the item to the database -->
    <Button
        android:id="@+id/add"
        android:layout_width="match_parent"
        android:layout_height="0dip"
        android:layout_weight=".1"
        android:text="Add" />

</LinearLayout>
